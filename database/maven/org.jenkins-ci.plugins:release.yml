---
- id: snykio:maven:org.jenkins-ci.plugins:release:2017-12-21
  title: Cross-site Request Forgery (CSRF)
  description: |
    [org.jenkins-ci.plugins:release][1] adds the ability to wrap your job
    with pre- and post- build steps which are only executed when a manual
    release build is triggered.

    Affected versions of this package are vulnerable to Cross-site Request
    Forgery (CSRF). It allows attackers to trigger release builds. Attackers
    were able to trigger release builds.

    \$$ Remediation Upgrade `org.jenkins-ci.plugins:release` to version 2.10
    or higher.



    [1]: https://github.com/jenkinsci/release-plugin
    \n[org.jenkins-ci.plugins:release][1] adds the ability to wrap your job
    with pre- and post- build steps which are only executed when a manual
    release build is triggered.

    Affected versions of this package are vulnerable to Cross-site Request
    Forgery (CSRF). It allows attackers to trigger release builds. Attackers
    were able to trigger release builds.

    \$$ Remediation Upgrade `org.jenkins-ci.plugins:release` to version 2.10
    or higher.



    [1]: https://github.com/jenkinsci/release-plugin
    \n[org.jenkins-ci.plugins:release][1] adds the ability to wrap your job
    with pre- and post- build steps which are only executed when a manual
    release build is triggered.

    Affected versions of this package are vulnerable to Cross-site Request
    Forgery (CSRF). It allows attackers to trigger release builds. Attackers
    were able to trigger release builds.

    \$$ Remediation Upgrade `org.jenkins-ci.plugins:release` to version 2.10
    or higher.



    [1]: https://github.com/jenkinsci/release-plugin
    \n[org.jenkins-ci.plugins:release][1] adds the ability to wrap your job
    with pre- and post- build steps which are only executed when a manual
    release build is triggered.

    Affected versions of this package are vulnerable to Cross-site Request
    Forgery (CSRF). It allows attackers to trigger release builds. Attackers
    were able to trigger release builds.

    \$$ Remediation Upgrade `org.jenkins-ci.plugins:release` to version 2.10
    or higher.



    [1]: https://github.com/jenkinsci/release-plugin
  affected_package: org.jenkins-ci.plugins:release
  vulnerable_versions:
  - "< 2.10"
  severity: high
  package_manager: maven
  cve:
  - CVE-2018-1000013
  cwe:
  - CWE-352
  credit:
  - Jesse Glick
  references:
  - https://jenkins.io/security/advisory/2018-01-22/
  - https://github.com/jenkinsci/release-plugin/commit/fe1660a5ebff2377b6a64214c6012e8615b3f81f
  - https://nvd.nist.gov/vuln/detail/CVE-2018-1000013
  source_url: https://snyk.io/vuln/SNYK-JAVA-ORGJENKINSCIPLUGINS-32165
