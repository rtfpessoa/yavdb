---
- id: rubyadvisory:rubygems:active-support:2018-08-09
  title: Malicious ruby gem - active-support
  description: |
    The gem duplicates official `activesupport` (no hyphen) code, but adds a
    compiled extension. The extension attempts to resolve a base64 encoded
    domain, downloads a payload, and executes.

    Replace this gem with the official `activesupport` gem.
  affected_package: active-support
  vulnerable_versions:
  - "*"
  package_manager: rubygems
  cve:
  - CVE-2018-3779
  credit:
  - Rubysec
  references:
  - https://github.com/rubysec/ruby-advisory-db
  - https://github.com/rubygems/rubygems.org/pull/1762
  source_url: https://hackerone.com/reports/392311
- id: snykio:rubygems:active-support:2018-08-09
  title: Malicious Package
  description: |+
    The `active-support` gem duplicates official activesupport code, but
    adds a compiled extension. This extension attempts to resolve a base64
    encoded domain, then downloads a payload and execute it.

    \nThe `active-support` gem duplicates official activesupport code, but
    adds a compiled extension. This extension attempts to resolve a base64
    encoded domain, then downloads a payload and execute it.

  affected_package: active-support
  vulnerable_versions:
  - ">=0.0.0"
  severity: high
  package_manager: rubygems
  cve:
  - CVE-2018-3779
  cwe:
  - CWE-506
  credit:
  - Unknown
  references:
  - https://hackerone.com/reports/392311
  source_url: https://snyk.io/vuln/SNYK-RUBY-ACTIVESUPPORT-22046
